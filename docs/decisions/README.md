# Architecture decisions

Add concise numbered decision records here when an implementation choice would
otherwise be easy to revisit accidentally. The product-level decisions already
approved in `PROJECT_PLAN.md` do not need to be duplicated.
